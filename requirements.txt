# =============================================================================
# Зависимости проекта orm_abc
# Обновлено до Django 5.2 LTS (апрель 2025)
#
# Установка:
#   pip install -r requirements.txt
#
# Документация Django 5.2:
#   https://docs.djangoproject.com/en/5.2/
# =============================================================================

# Основной веб-фреймворк (LTS-версия с поддержкой до апреля 2028)
Django==5.2

# ASGI-сервер — обеспечивает асинхронную поддержку Django
asgiref==3.8.1

# Утилита форматирования SQL-запросов, используется Django для отображения
# запросов в отладочном режиме
sqlparse==0.5.3

# Библиотека часовых поясов для Windows-систем (на Linux использует системную tzdata)
tzdata==2024.2

# Расширение для гибкой настройки виджетов форм Django прямо в шаблонах
django-widget-tweaks==1.5.0
